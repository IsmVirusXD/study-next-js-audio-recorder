export function renderTranscrption(biblioteca : string)
{
    switch (biblioteca) {
        case "SpeechRecognition":
            return <></>
    
        default:
            <p>Nenhum Gravador Encontrado</p>;
    }

}